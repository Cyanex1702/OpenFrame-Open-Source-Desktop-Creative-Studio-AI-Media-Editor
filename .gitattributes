# Required Windows FFmpeg runtime binaries are large third-party payloads.
# Keep them in the repository through Git LFS rather than normal Git objects.
vendor/ffmpeg/bin/*.exe filter=lfs diff=lfs merge=lfs -text
